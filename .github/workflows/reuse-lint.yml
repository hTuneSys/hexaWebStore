# SPDX-FileCopyrightText: 2025 hexaTune LLC
# SPDX-License-Identifier: MIT

name: 📄 REUSE Compliance

on: [pull_request]

jobs:
  reuse:
    name: REUSE Compliance (hexaWebStore)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install reuse
      - run: reuse lint
